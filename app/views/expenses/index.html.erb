<h1>All expenses</h1>

<table>
  <tr>
    <th>Date</th>
    <th>Description</th>
    <th>Value</th>
  </tr>
  <% @expenses.each do |expense| %>
    <tr>
      <td><%= expense.date %></td>
      <td><%= expense.description %></td>
      <td><%= expense.value %></td>
    </tr>
  <% end %>
</table>
